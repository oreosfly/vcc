import Router from 'koa-router';
const router = new Router({
    prefix: "/Bapi/v1/"
})
router.get('/verify')
router.get('/meta',)
router.post('/meta')
router.get('/entity')
router.post('/entity')

export default router

